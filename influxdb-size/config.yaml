name: "HA InfluxDB Size Monitor"
slug: "influxdb_size"
version: "1.1.0"
description: "Reports the size of the InfluxDB add-on data directory via MQTT with auto-discovery"
startup: services
boot: auto
host_network: true
homeassistant_api: false
hassio_api: true
hassio_role: default
options:
  interval: 1800
  mqtt_prefix: "homeassistant"
schema:
  interval: int
  mqtt_prefix: str
